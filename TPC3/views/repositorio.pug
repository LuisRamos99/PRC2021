extends layout

block content
    .w3-card-4
      header.w3-container.w3-teal
        h3.w3-left Repositorio #{rep.tit}
      if rep.length > 0 
        each b in rep
          ul
            li 
              b Classe: 
              |  
              a(href=`/repositorio/${id}/${b.s.value.split("#")[1]}`) #{b.s.value.split("#")[1]} 
      else 
        div(class="w3-container")
           p Não foi possível encontrar o repositório ... 
      a(href=`/repositorios` ) 
        button(class="w3-btn w3-teal" type="submit" style="margin:20px") Voltar ao inicio 
    footer(class="w3-container w3-teal")
        address Gerado por galuno :: 2021-Março, Luis Ramos