extends layout

block content
    .w3-card-4
      header.w3-container.w3-teal
        h3.w3-left Individuo "#{individuo}"
      if lista.length > 0 
        each b in lista
          ul
            li 
              if (b.o.value.split("#")[1]) 
                b #{b.p.value.split("#")[1]} : 
                | #{b.o.value.split("#")[1]} 
              else 
                b #{b.p.value.split("#")[1]} : 
                | #{b.o.value} 
      else 
        div(class="w3-container")
           p Não foi possível encontrar o individuo ... 
      a(href=`/repositorios` ) 
        button(class="w3-btn w3-teal" type="submit" style="margin:20px") Voltar ao inicio
    footer(class="w3-container w3-teal")
        address Gerado por galuno :: 2021-Março, Luis Ramos